# 农历生日提醒应用 (Kimi 版本)

一个可爱的农历生日提醒应用,支持多语言、日历视图和生日管理。

## 功能特点

- 🎂 农历生日追踪和转换
- 📅 月历视图显示所有生日
- 🔔 即将到来的生日提醒(14天内)
- 🌍 多语言支持(简体中文、繁体中文、English)
- 🎨 可爱的 UI 设计,使用 Tailwind CSS
- 💾 本地存储数据,无需后端
- 📱 响应式设计,支持移动端

## 项目结构

```
kimi/
├── src/
│   ├── components/       # React 组件
│   │   ├── Header.tsx          # 头部组件(标题、语言切换)
│   │   ├── Sidebar.tsx         # 侧边栏(提醒列表、小伙伴列表)
│   │   ├── Calendar.tsx        # 日历组件
│   │   └── Modals.tsx          # 模态框组件(详情、添加表单)
│   ├── hooks/           # 自定义 Hooks
│   │   ├── useBirthdays.ts     # 生日数据管理
│   │   └── useLanguage.ts      # 语言设置管理
│   ├── utils/           # 工具函数
│   │   ├── lunar.ts            # 农历转换工具
│   │   └── i18n.ts             # 国际化翻译
│   ├── App.tsx          # 主应用组件
│   ├── main.tsx         # 应用入口
│   ├── types.ts         # TypeScript 类型定义
│   └── index.css        # 全局样式
├── index.html           # HTML 模板
├── package.json         # 项目配置
├── tsconfig.json        # TypeScript 配置
├── vite.config.ts       # Vite 配置
├── tailwind.config.js   # Tailwind CSS 配置
└── README.md            # 项目说明

```

## 开发指南

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

### 构建生产版本

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

## 二次开发说明

### 添加新功能

1. **添加新的翻译**: 在 `src/utils/i18n.ts` 中的 `translations` 对象添加新的翻译键值对
2. **创建新组件**: 在 `src/components/` 目录下创建新的组件文件
3. **添加新的工具函数**: 在 `src/utils/` 目录下添加工具函数
4. **创建自定义 Hook**: 在 `src/hooks/` 目录下创建自定义 Hook

### 修改样式

- 全局样式: 编辑 `src/index.css`
- 组件样式: 使用 Tailwind CSS 类名
- 自定义主题: 编辑 `tailwind.config.js`

### 数据持久化

当前使用 `localStorage` 存储数据,如需改为后端存储:

1. 修改 `src/hooks/useBirthdays.ts` 中的 `useEffect`
2. 添加 API 调用逻辑
3. 添加错误处理和加载状态

### 添加新的节日

编辑 `src/utils/lunar.ts` 中的 `getHoliday` 函数

## 技术栈

- **框架**: React 19.2.4 + TypeScript
- **构建工具**: Vite 6.2.0
- **样式**: Tailwind CSS 3.4.17
- **动画**: Framer Motion 11.11.11
- **图标**: Lucide React 0.454.0
- **农历库**: lunar-javascript 1.6.12

## 浏览器支持

- Chrome (最新版本)
- Firefox (最新版本)
- Safari (最新版本)
- Edge (最新版本)

## 许可证

MIT
